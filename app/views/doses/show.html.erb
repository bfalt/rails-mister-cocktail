<%= @cocktail.name %>
<% @doses.each do |dose| %>
  <%= dose.description %> <%= dose.ingredient.name %>
  <%= link_to "Delete Dose", dose_path(dose), method: :delete %>
  <% end %>

  <%= link_to "Add a dose to this Cocktail", new_dose_path(@cocktail)  %>
  <%= link_to "List of Cocktails", cocktail_path  %>
